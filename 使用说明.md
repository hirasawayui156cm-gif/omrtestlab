# OMR 多链路聚合测试台 —— 使用说明

一套用于 OpenMPTCProuter（OMR）多链路聚合实验的自动化测试工具。

- **运行位置**：Windows 管理电脑（Python 3.11+）
- **后台**：Python + FastAPI
- **界面**：浏览器 Web 页面（图表用 ECharts）
- **控制对象**：通过 SSH 远程操作 PVE 中的 OMR 客户端（执行 tc 整形 + iperf3 客户端）和 Debian 镜像中的 OMR VPS 服务端（运行 iperf3 服务端）
- **测速**：iperf3　　**链路整形**：tc（tbf 限速 + netem 时延/抖动/丢包）

> 当前实验拓扑以 `omr.pptx` 为准：一台软路由的 PVE 中运行 OMR 客户端，另一台软路由的 Debian 镜像运行 OMR VPS 服务端，三条 WAN 链路连接两端。文档中出现的“香蕉派”可按你的实际 OMR 客户端理解。

## 当前界面与离线调试

项目包含两个独立界面，职责不同：

- **OMR 多链路聚合实验台**：负责环境配置、实验设计、CORE/`tc` 整形、iperf3 测试、实时监控和结果导出。
- **OMR 多链路聚合控制台**：负责设备拓扑、接口草稿、接入流程和控制面展示。

当前未接入真实设备时，两个界面都处于离线调试模式。页面可以直接打开、切换主题、编辑实验参数和查看交互流程，但不会自动连接远端设备。

### 启动本地调试地址

在项目根目录 `D:\Multi-link\omrtestlab` 启动实验台：

```powershell
python run.py --host 0.0.0.0 --port 8000
```

浏览器打开：`http://127.0.0.1:8000`

控制台界面位于 `D:\Multi-link\vps-ui`，另开终端启动：

```powershell
cd D:\Multi-link\vps-ui
node server.mjs
```

浏览器打开：`http://127.0.0.1:4173`

两个界面右上角都可以在浅色和深色主题之间切换，选择会保存在当前浏览器中。实验台的图表依赖 ECharts CDN，离线时图表可能无法加载，但配置和日志界面仍可调试。

> 安全提示：SSH 凭据只应在本地调试环境使用。页面不会回显已保存的密码，后端保存配置时留空密码表示保持原凭据。正式接入设备前建议改用 SSH 密钥并轮换测试凭据。

## 目录结构

```
omr-testlab/
├── run.py                 # 启动入口
├── requirements.txt       # 依赖
├── app/
│   ├── server.py          # FastAPI 服务（REST + WebSocket + 静态页面）
│   ├── runner.py          # 后台测试运行器（整形→测速→采集→统计→清理→入库）
│   ├── ssh.py             # SSH 封装（paramiko）
│   ├── tc.py              # tc 整形命令生成
│   ├── iperf.py           # iperf3 执行与结果解析
│   ├── monitor.py         # 链路吞吐实时监控 + RTT 测量
│   ├── config.py          # 配置读写
│   ├── storage.py         # 结果持久化（SQLite）
│   └── static/            # 前端页面（index.html / app.js / style.css）
└── data/                  # settings.json（配置+实验设计）、results.db（结果）
```

## 启动

```powershell
cd D:\Multi-link\omrtestlab
python -m pip install -r requirements.txt
python run.py --port 8000
```

浏览器打开：`http://127.0.0.1:8000`

> 页面图表依赖 ECharts CDN，首次打开需联网加载；若加载失败，表格和日志不受影响。

## 前置条件（设备侧）

1. 香蕉派（OpenWrt/OMR）已连通 VPS，多链路隧道全部 `Connected`。
2. 香蕉派可执行 `tc`（OpenWrt 默认带），iperf3 可在页面里"一键准备"自动安装。
3. VPS 防火墙/安全组放行 iperf3 端口（默认 **5201 UDP**）。

---

## 第 1 步：环境配置页签（必做）

这一页是"接线"，告诉程序去哪里控制设备。**不填这里，后面什么都跑不起来。**

### 路由器（香蕉派 OMR 客户端）

| 字段 | 填什么 |
|---|---|
| 主机地址 | 香蕉派管理地址，默认 `192.168.100.1` |
| SSH 端口 | 默认 `22` |
| 用户名 | OpenWrt 默认 `root` |
| 密码 | 初始化向导里设置的 root 密码 |
| 使用 sudo | OpenWrt 用 root 登录时**不用勾**；普通用户才勾 |

- **测试 SSH 连接**：能连上会显示"连接成功"。
- **检测 WAN 接口**：列出路由器所有网卡（eth0/eth1/…）。**这些接口名后面设计实验组时要用，务必先做。**

### VPS（OMR 服务端）

- 填 VPS 公网 IP、SSH 端口(22)、用户名(root)、密码，可测试连接。
- **iperf3 端口**默认 `5201`，必须与 VPS 防火墙放行的一致。

### 一键准备环境

点"检查并准备"，依次执行：
1. 香蕉派检查/安装 iperf3（`opkg install iperf3`）
2. VPS 检查/安装 iperf3（`apt install iperf3`）
3. VPS 启动 iperf3 服务端（`iperf3 -s -D`）

> VPS 连不上时，九成是安全组没放行 5201 的 UDP/TCP。

## 第 2 步：实验设计页签

设计"要做哪几组实验、每组怎么整链路"。**支持多组，用于后面对比。**

### 添加实验组

点右上角"+ 添加实验组"创建一组，每个实验组包含：

**顶部一行：**
- **实验组名称**：建议写成可读描述，如 `链路1=100M/20ms 链路2=50M/40ms`
- **时长(s)**：每次 iperf3 测速秒数（TCP 会跑上行+下行各一次，共 2 倍时长）
- **协议**：`TCP（上行+下行）` 或 `UDP（含抖动/丢包统计）`
- **UDP带宽(M)**：仅 UDP 有效，0 表示按各链路限速之和自动算

**链路表格（一行一条 WAN）：**

| 列 | 含义 |
|---|---|
| 启用 | 勾上这条链路参与聚合测试 |
| 链路名 | 随意，仅用于显示 |
| 接口 | 从下拉选第 1 步检测到的网卡名 |
| 限速 Mbps | **核心参数**，给这条链路设定的带宽上限（tc tbf） |
| 时延 ms | 附加单向时延（tc netem） |
| 抖动 ms | 时延抖动范围 |
| 丢包 % | 丢包率 |

`+ 添加链路` 可加更多链路；`×` 删除该链路。

**断链/容灾测试（可选）：**
- 勾选后，测速中途会**真实断开某个接口**，观察 MPTCP 是否降速不断流。
- 断开接口 / 断开时间点(s) / 恢复间隔(s)（0 表示测速结束前不恢复）。
- 程序自动估算"吞吐恢复到断链前 85% 的耗时"（表格"恢复s"列）。

### 保存设计

改完点 **"保存设计"**，存入 `data/settings.json`，下次打开还在。

> 建议建 3~4 组对比实验，例如：
> - 基线组：链路1=100M，链路2=50M
> - 大时延组：链路1=100M/50ms，链路2=50M/50ms
> - 高丢包组：链路1=100M/0ms/2%丢包，链路2=50M/0ms/5%丢包
> 便于直接看出时延/丢包对聚合效率的影响。

## 第 3 步：运行测试页签

- 勾选要跑的实验组（默认全选）→ 点 **▶ 开始运行**，之后全程自动，每组流程：
  1. 对每条启用链路执行 `tc` 整形（限速+时延+丢包）
  2. 启动实时监控线程（每 0.5s 读路由器 `/proc/net/dev`）
  3. 跑上行 iperf3 → 下行 iperf3（TCP），或 UDP
  4. 对每条链路 `ping` 测量实际 RTT
  5. 停止监控，汇总统计（每链路平均/峰值速率、聚合效率%）
  6. **自动清除 tc 整形规则**（不留残留）
  7. 结果入库并实时推送
- 中途可点 **■ 停止**（当前 iperf3 尽快中止后清理整形）。

**实时看什么：**
- 右上角状态灯：运行中绿色闪烁。
- 实时监控图：各时刻上行/下行聚合总吞吐（来自路由器实际网卡计数）。
- 运行日志：滚动显示每条 tc 命令、测速结果、断链事件，出错标红。

## 第 4 步：结果分析页签

### 上方 4 张对比图（横轴 = 每个实验组）

| 图 | 看什么 |
|---|---|
| 聚合吞吐对比 | 每组上行/下行 Mbps |
| 聚合效率对比 | 实测值÷理论限速和×100%，**最能说明聚合质量**（80~95% 正常） |
| 各链路负载分布 | 每条链路实际承载流量堆叠图，看流量是否摊到两条链路 |
| 各链路时延 | 实测每链路 RTT 对比 |

### 下方明细表格

每行一个实验结果，**点任意行**打开该组明细：
- iperf3 逐秒吞吐时间线（上行/下行）
- 各链路逐秒实时吞吐（监控数据）
- 每链路"配置 vs 实测"对照表
- 断链/恢复记录及恢复耗时

### 导出

- **导出 CSV**：一行一个实验组，含全部关键指标，Excel 可直接打开。
- **导出 JSON**：全部原始数据（时间线、监控、链路明细），用于写报告或二次分析。
- **清空结果**：删除所有历史结果。

---

## 一句话流程

环境配置填两端 SSH → 检测接口 → 一键准备 → 设计若干实验组 → 保存 → 勾选运行 → 结果页看对比图表和表格 → 导出。

## 常见问题

| 现象 | 原因/处理 |
|---|---|
| 页面图表空白 | ECharts CDN 未加载，检查网络或更换可用源 |
| 测试 SSH 连接失败 | 核对主机/IP/端口/密码，确认路由器 SSH 已开启 |
| 一键准备 VPS 失败 | VPS 安全组未放行 5201 UDP/TCP |
| 测速结果显示 0 | iperf3 未装上/端口不对；或链路本身不通 |
| 聚合带宽不理想 | 见实验文档 FAQ：VPS 出口带宽是天花板，可试切换 glorytun-udp |
| 运行中想全部停止 | 点 ■ 停止，程序会中止当前任务并清理 tc 规则 |
